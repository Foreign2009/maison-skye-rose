/**
 * Knowledge Factory — Version
 *
 * Single authoritative factory version constant.
 * All factory modules that reference the current production version import from here.
 * Update this value when the factory contract version changes.
 */

export const FACTORY_VERSION = "0.5.0";
